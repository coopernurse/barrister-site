package example;

// Calculator is a generated file based on the IDL Calculator interface
// 
// Server is exposed via the provided BarristerServlet, which lets you
public class Server implements Calculator {

    public Double add(Double a, Double b) {
        return a+b;
    }

    public Double subtract(Double a, Double b) {
        return a-b;
    }

}